import React, { useState } from 'react';

/* passing the updateCity state updater from parent to child component */
const Search = ({ updateSearchCity }) => {
// Create a state for input fields

  // Implement search handling logic
  const searchHandler = (e) => {

  };

  // Implement submit handling logic
  const submitHandler = (e) => {

  };

  return (
    <div>
      {/* Create input fields to search by city name*/}
    </div>
  );
};

export default Search;